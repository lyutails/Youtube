import { WordsPipePipe } from './words-pipe.pipe';

describe('WordsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new WordsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
